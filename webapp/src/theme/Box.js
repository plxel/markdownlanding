import styled from "styled-components"
import {
  space,
  color,
  fontSize,
  border,
  borderRadius,
  layout,
  typography,
  flexbox,
} from "styled-system"

const Box = styled.div`
  ${space}
  ${color}
  ${fontSize}
  ${border}
  ${borderRadius}
  ${layout}
  ${typography}
  ${flexbox}
`

export default Box
